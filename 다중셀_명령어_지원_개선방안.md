# 다중 셀 명령어 지원을 위한 구조 개선 방안

## 🎯 현재 구조의 문제점

### 1. 단일 명령어 구조
- 현재 `formulaDetails.spreadjsCommand`는 하나의 문자열 명령어만 처리
- 다중 셀 작업 시 여러 명령어가 순차적으로 실행되어야 하는 상황 미지원

### 2. 백엔드 응답 구조 한계
- `spreadjsCommand` 필드가 단일 명령어 문자열로 제한
- 복잡한 다중 셀 작업의 트랜잭션 단위 처리 불가

### 3. 프론트엔드 실행 엔진 한계
- `executeCommand`가 단일 명령어 파싱에만 최적화
- 명령어 간 의존성 및 실행 순서 관리 부재

## 🔄 백엔드 응답 구조 개선

### 1. 기존 구조
```json
{
  "formulaDetails": {
    "spreadjsCommand": "worksheet.setFormula(0, 0, '=SUM(A1:A10)')"
  }
}
```

### 2. 개선된 구조
```json
{
  "formulaDetails": {
    "commandType": "batch | single",
    "commands": [
      {
        "id": "cmd_001",
        "type": "setFormula",
        "description": "A1에 합계 공식 적용",
        "command": "worksheet.setFormula(0, 0, '=SUM(B1:B10)')",
        "targetRange": "A1",
        "dependencies": [],
        "order": 1
      },
      {
        "id": "cmd_002", 
        "type": "setValue",
        "description": "B1에 값 설정",
        "command": "worksheet.setValue(0, 1, 100)",
        "targetRange": "B1",
        "dependencies": [],
        "order": 2
      }
    ],
    "execution": {
      "mode": "sequential | parallel | conditional",
      "rollbackStrategy": "all_or_nothing | partial_rollback",
      "validation": "pre_execution | post_execution | none"
    }
  }
}
```

## 🛠 프론트엔드 로직 변경 사항

### 1. FormulaResponse 타입 확장
```typescript
interface FormulaResponse {
  // 기존 필드들...
  formulaDetails: {
    // 기존 필드들...
    commandType: 'single' | 'batch';
    commands?: CommandDetail[];
    execution?: ExecutionConfig;
    spreadjsCommand?: string; // 하위호환성 유지
  };
}

interface CommandDetail {
  id: string;
  type: string;
  description: string;
  command: string;
  targetRange: string;
  dependencies: string[];
  order: number;
  retryCount?: number;
  timeout?: number;
}
```

### 2. 배치 명령어 실행 엔진 추가
```typescript
// useSpreadjsCommandEngine.ts 내부
const executeBatchCommands = useCallback(async (commands: CommandDetail[]) => {
  // 1. 의존성 그래프 생성
  // 2. 실행 순서 정렬
  // 3. 순차/병렬 실행 처리
  // 4. 에러 발생 시 롤백 처리
  // 5. 부분 성공 처리
});
```

### 3. 트랜잭션 관리 강화
```typescript
interface BatchExecutionResult {
  overallSuccess: boolean;
  totalCommands: number;
  successfulCommands: number;
  failedCommands: CommandExecutionError[];
  rollbackRequired: boolean;
  executionTime: number;
  commandResults: Array<{
    commandId: string;
    success: boolean;
    result?: any;
    error?: string;
  }>;
}
```

## 📝 프롬프트 수정 방향

### 1. operationType 확장
```json
{
  "analysis": {
    "operationType": "single_cell | multiple_cells | range_operation | batch_operation | complex_workflow"
  }
}
```

### 2. 다중 명령어 지원 섹션 추가
```markdown
**📊 다중 명령어 배치 처리:**
다음과 같은 경우 commandType을 "batch"로 설정하고 commands 배열 사용:

1. **여러 셀에 서로 다른 공식 적용**
2. **데이터 입력 + 계산 + 서식 적용 조합**
3. **조건부 작업 (if-then 로직)**
4. **의존성이 있는 순차 작업**

배치 명령어 예시:
```json
{
  "commandType": "batch",
  "commands": [
    {
      "id": "step1",
      "type": "setValue", 
      "command": "worksheet.setValue(0, 0, '총계')",
      "order": 1
    },
    {
      "id": "step2",
      "type": "setFormula",
      "command": "worksheet.setFormula(0, 1, '=SUM(B2:B10)')",
      "order": 2,
      "dependencies": ["step1"]
    }
  ]
}
```
```

### 3. 명령어 타입별 가이드라인 확장
```markdown
**🔄 복합 작업 패턴:**

**패턴 1: 데이터 입력 → 계산 → 서식**
- Step 1: setValue로 기본 데이터 입력
- Step 2: setFormula로 계산 수식 적용  
- Step 3: setStyle로 결과 셀 강조

**패턴 2: 조건부 다중 처리**
- 특정 조건의 여러 셀에 각각 다른 처리
- 범위별로 다른 공식이나 값 적용

**패턴 3: 종속적 계산**
- A 계산 결과를 B 계산에 활용
- 순차적 실행이 필요한 경우
```

### 4. 실행 모드 지정 가이드
```markdown
**실행 모드 선택 기준:**
- **sequential**: 명령어 간 의존성이 있는 경우
- **parallel**: 독립적인 여러 셀 처리 시
- **conditional**: 조건에 따른 분기 처리 필요 시

**롤백 전략:**
- **all_or_nothing**: 하나라도 실패 시 전체 롤백
- **partial_rollback**: 성공한 것은 유지, 실패한 것만 롤백
```

## 🔧 구현 우선순위

### Phase 1: 기본 배치 처리
1. 백엔드 응답 구조 확장
2. 프론트엔드 타입 정의 업데이트  
3. 단순 순차 실행 엔진 구현

### Phase 2: 고급 기능
1. 의존성 관리 시스템
2. 병렬 실행 최적화
3. 부분 롤백 메커니즘

### Phase 3: 최적화
1. 성능 모니터링
2. 에러 복구 전략 고도화
3. 사용자 피드백 시스템

## 🎯 기대 효과

### 1. 복잡한 작업 지원
- 단일 요청으로 여러 셀 동시 처리
- 데이터 입력부터 분석까지 원스톱 처리

### 2. 안정성 향상  
- 트랜잭션 단위 처리로 데이터 일관성 보장
- 실패 시 자동 롤백으로 데이터 손실 방지

### 3. 성능 최적화
- 배치 처리로 네트워크 왕복 횟수 감소
- 병렬 실행으로 처리 시간 단축

### 4. 사용자 경험 개선
- 복잡한 요청을 자연어로 한 번에 처리
- 진행 상황 실시간 피드백 제공

## 🔄 구현 세부 계획

### 백엔드 변경사항

#### 1. API 응답 스키마 확장
```typescript
interface ExtendedFormulaResponse {
  success: boolean;
  analysis: {
    detectedOperation: string;
    dataRange: string;
    targetCells: string;
    operationType: "single_cell" | "multiple_cells" | "range_operation" | "batch_operation" | "complex_workflow";
    affectedRanges: string[]; // 영향받는 모든 셀 범위
  };
  formulaDetails: {
    name: string;
    description: string;
    syntax: string;
    commandType: "single" | "batch";
    // 단일 명령어 (하위호환성)
    spreadjsCommand?: string;
    // 배치 명령어
    commands?: CommandDetail[];
    execution?: ExecutionConfig;
  };
  implementation: {
    steps: string[];
    cellLocations: {
      source: string;
      target: string;
      description: string;
    };
    estimatedExecutionTime?: number;
  };
}
```

#### 2. AI 프롬프트 템플릿 업데이트
```markdown
**다중 명령어 처리 가이드:**

사용자 요청이 다음 중 하나에 해당하면 commandType을 "batch"로 설정:
- 여러 셀에 서로 다른 값이나 공식 적용
- 데이터 입력 후 계산 및 서식 적용이 필요한 경우
- 조건에 따라 다른 처리가 필요한 경우
- 단계적으로 처리해야 하는 복잡한 작업

배치 명령어 작성 예시:
```json
{
  "commandType": "batch",
  "commands": [
    {
      "id": "init_header",
      "type": "setValue",
      "description": "헤더 셀에 제목 입력",
      "command": "worksheet.setValue(0, 3, '부서별 총계', GC.Spread.Sheets.SheetArea.viewport)",
      "targetRange": "D1",
      "dependencies": [],
      "order": 1
    },
    {
      "id": "calc_sales",
      "type": "setFormula",
      "description": "영업팀 매출 합계 계산",
      "command": "worksheet.setFormula(1, 3, '=SUMIF(B:B,\"영업팀\",C:C)', GC.Spread.Sheets.SheetArea.viewport)",
      "targetRange": "D2",
      "dependencies": ["init_header"],
      "order": 2
    },
    {
      "id": "style_result",
      "type": "setStyle",
      "description": "결과 셀 강조 표시",
      "command": "var style = new GC.Spread.Sheets.Style(); style.backColor = '#FFFF99'; style.fontWeight = 'bold'; worksheet.setStyle(1, 3, style, GC.Spread.Sheets.SheetArea.viewport)",
      "targetRange": "D2",
      "dependencies": ["calc_sales"],
      "order": 3
    }
  ],
  "execution": {
    "mode": "sequential",
    "rollbackStrategy": "all_or_nothing",
    "validation": "post_execution"
  }
}
```
```

### 프론트엔드 변경사항

#### 1. 타입 정의 확장
```typescript
// types/spreadsheet.ts
interface CommandDetail {
  id: string;
  type: 'setValue' | 'setFormula' | 'setStyle' | 'sortRange' | 'copyTo' | 'moveTo';
  description: string;
  command: string;
  targetRange: string;
  dependencies: string[];
  order: number;
  retryCount?: number;
  timeout?: number;
  validation?: 'none' | 'syntax' | 'data' | 'full';
}

interface ExecutionConfig {
  mode: 'sequential' | 'parallel' | 'conditional';
  rollbackStrategy: 'all_or_nothing' | 'partial_rollback' | 'continue_on_error';
  validation: 'pre_execution' | 'post_execution' | 'none';
  maxRetries?: number;
  timeoutMs?: number;
}

interface BatchExecutionContext {
  executedCommands: string[];
  snapshots: Map<string, any>;
  errors: Array<{commandId: string; error: string}>;
  startTime: number;
}
```

#### 2. 배치 실행 엔진 구현
```typescript
// hooks/useSpreadjsCommandEngine.ts 확장
const executeBatchCommands = useCallback(async (
  commands: CommandDetail[],
  execution: ExecutionConfig,
  response: FormulaResponse
): Promise<BatchExecutionResult> => {
  const context: BatchExecutionContext = {
    executedCommands: [],
    snapshots: new Map(),
    errors: [],
    startTime: Date.now()
  };

  try {
    // 1. 의존성 그래프 생성 및 정렬
    const sortedCommands = resolveDependencies(commands);
    
    // 2. 실행 모드에 따른 처리
    switch (execution.mode) {
      case 'sequential':
        return await executeSequentially(sortedCommands, context, execution);
      case 'parallel':
        return await executeInParallel(sortedCommands, context, execution);
      case 'conditional':
        return await executeConditionally(sortedCommands, context, execution);
    }
  } catch (error) {
    // 롤백 처리
    await handleRollback(context, execution.rollbackStrategy);
    throw error;
  }
});
```

#### 3. 의존성 관리 시스템
```typescript
const resolveDependencies = (commands: CommandDetail[]): CommandDetail[] => {
  // 위상 정렬 알고리즘으로 실행 순서 결정
  const graph = buildDependencyGraph(commands);
  return topologicalSort(graph);
};

const buildDependencyGraph = (commands: CommandDetail[]) => {
  // 명령어 간 의존성 그래프 생성
  const graph = new Map<string, string[]>();
  const inDegree = new Map<string, number>();
  
  commands.forEach(cmd => {
    graph.set(cmd.id, cmd.dependencies);
    inDegree.set(cmd.id, cmd.dependencies.length);
  });
  
  return { graph, inDegree };
};
```

## 🔍 테스트 시나리오

### 시나리오 1: 부서별 매출 분석
```json
{
  "userRequest": "부서별 매출을 계산하고 결과를 강조해서 보여줘",
  "expectedResponse": {
    "commandType": "batch",
    "commands": [
      {"type": "setValue", "description": "헤더 추가"},
      {"type": "setFormula", "description": "영업팀 매출 합계"},
      {"type": "setFormula", "description": "마케팅팀 매출 합계"},
      {"type": "setStyle", "description": "결과 셀 강조"}
    ]
  }
}
```

### 시나리오 2: 데이터 검증 및 수정
```json
{
  "userRequest": "빈 셀이 있으면 0으로 채우고 전체 합계를 구해줘",
  "expectedResponse": {
    "commandType": "batch",
    "execution": {
      "mode": "conditional",
      "rollbackStrategy": "partial_rollback"
    }
  }
}
```

### 시나리오 3: 복합 보고서 생성
```json
{
  "userRequest": "월별 매출 분석 보고서를 만들어줘",
  "expectedResponse": {
    "commandType": "batch",
    "commands": [
      {"type": "setValue", "description": "보고서 제목"},
      {"type": "setFormula", "description": "월별 합계"},
      {"type": "setFormula", "description": "평균 계산"},
      {"type": "setStyle", "description": "보고서 서식"},
      {"type": "sortRange", "description": "매출순 정렬"}
    ]
  }
}
```

## 💡 추가 고려사항

### 1. 성능 최적화
- 명령어 그룹핑으로 SpreadJS API 호출 최소화
- 대용량 데이터 처리 시 청크 단위 처리
- 비동기 실행으로 UI 블로킹 방지

### 2. 사용자 경험
- 진행률 표시기 제공
- 실시간 실행 상태 피드백
- 에러 발생 시 사용자 친화적 메시지

### 3. 확장성
- 플러그인 아키텍처로 새로운 명령어 타입 추가 용이
- 커스텀 검증 로직 지원
- 외부 시스템 연동 인터페이스 제공

### 4. 보안 및 안정성
- 명령어 실행 전 검증 강화
- 리소스 사용량 모니터링
- 악의적 명령어 패턴 감지

이러한 개선을 통해 현재의 단일 명령어 처리 한계를 극복하고, 복잡한 다중 셀 작업을 안정적이고 효율적으로 처리할 수 있는 시스템을 구축할 수 있습니다.